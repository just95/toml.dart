name: example
description: An example that demonstrates how to encode a TOML document.
repository: https://github.com/just95/toml.dart/tree/main/example/toml_parser
publish_to: none
environment:
  sdk: '>=2.17.0 <3.0.0'
dependencies:
  toml:
    path: ../../
dev_dependencies:
  lints: ^2.0.0
